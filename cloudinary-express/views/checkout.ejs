
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checkout Page</title>
  <style>
    body {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Verdana, sans-serif;
    background-color: #FFA500;
    color: #333;
    text-align: center;
    }

    .banner {
    width: 100%;
    background-color: #FF8C00;
    padding: 10px;
    box-shadow: 0px 5px 5px rgba(0, 0, 0, 0.2);
    }

    h1, h2 {
    color: white;
    margin: 0;
    }

    .items-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    margin-top: 20px;
    }

    .item {
    max-width: 300px;
    padding: 20px;
    border: 1px solid #ddd;
    border-radius: 8px;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .item h3 {
    color: #333;
    }

    .total-price {
    margin-top: 20px;
    font-size: 24px;
    }

    .payment {
    margin-top: 20px;
    }

    a.button {
    display: inline-block;
    padding: 10px 20px;
    background-color: #FF6347;
    color: white;
    text-decoration: none;
    border-radius: 8px;
    transition: background-color 0.3s;
    }

    a.button:hover {
    background-color: #FF4500;
    }

  </style>
</head>
<body>
  <div class="banner">
    <h1>Welcome to YSL</h1>
    <h2>Where shopping never ends~</h2>
  </div>

  <h1>Checkout</h1>

  <div class="items-container">
    <% items.forEach(item => { %>
      <div class="item">
        <h3><%= item.productName %></h3>
        <p>Quantity: <%= item.quantity %></p>
        <p>Price: <%= item.price %></p>
        <p>Category: <%= item.category %></p>
      </div>
    <% }); %>
  </div>

  <p class="total-price">Total Price: <%= totalPrice %></p>

  <div class="payment">
    <a href="/checkout/payment" class="button">Proceed to Payment</a>
  </div>

</body>
</html>
